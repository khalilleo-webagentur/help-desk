{% extends 'dashboard/layout/base.html.twig' %}

{% block title %} Dashboard Ticket Labels {% endblock %}

{% block body %}

    <div class="content-wrapper">

        <div class="content-header">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-6 mt-1">
                        <h5 class="m-0 text-dark mt-1">Ticket Labels</h5>
                    </div>
                    <div class="col-sm-6 mt-1">
                        <div class="breadcrumb float-right">
                            {% if is_granted('ROLE_SUPER_ADMIN') %}
                                <button class="btn btn-outline-secondary"
                                        data-toggle="modal" data-target="#newLabelModal">
                                    <span class="fa fa-plus fa-sm"></span> <span class="ps-1">Add new Label</span>
                                </button>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <section class="content">
            <div class="container-fluid">

                <div class="row">
                    <div class="col-lg-12">
                        {% include 'dashboard/ticket-labels/partial/table_content.html.twig' %}
                    </div>
                </div>

                <div class="row">
                    <section class="col-lg-7 connectedSortable"></section>
                    <section class="col-lg-5 connectedSortable"></section>
                </div>
            </div>
        </section>
    </div>

    {% include 'dashboard/ticket-labels/modal/new.html.twig' %}

{% endblock %}